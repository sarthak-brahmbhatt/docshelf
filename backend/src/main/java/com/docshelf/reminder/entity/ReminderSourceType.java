// What a reminder is derived from (reminder.source_type / reminder_rule.source_type)
package com.docshelf.reminder.entity;

public enum ReminderSourceType { DOCUMENT, FAMILY_MEMBER, MEDICINE, DOCUMENT_EVENT }
